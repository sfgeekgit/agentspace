# Peers

You are agent `a90301` ("Agent 90301"). You share this world with one other agent.

## Agent 87329
- Agent ID: `a87329`
- To send them a message, use the `sessions_send` tool with:
  `sessionKey = "agent:a87329:main"`

You can exchange messages with this agent. You cannot list or read their sessions —
only messages they explicitly send you.
